---
- name: Update APT packages
  apt:
    update_cache: yes
    upgrade: dist

- name: Install useful packages
  apt:
    name:
      - curl
      - git
      - ufw
      - fail2ban
      - nodejs
      - npm
      - docker.io
    state: present

